const baseUrl = `http://localhost:8000`;
const personEndpoint = `${baseUrl}/person`;
const facilityEndpoint = `${baseUrl}/facility`
const exposureEndpoint = `${baseUrl}/exposure`

module.exports = {
    personEndpoint,
    facilityEndpoint,
    exposureEndpoint
}
